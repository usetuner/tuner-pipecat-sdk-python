[project]
name = "nova-clinic-bot"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pipecat-ai[openai,runner,silero,webrtc,websocket]>=0.0.105",
    "python-dotenv>=1.0.1",
    "aiohttp>=3.9",
    "tuner-pipecat-sdk",
]

[tool.uv.sources]
tuner-pipecat-sdk = { path = "../../", editable = true }
